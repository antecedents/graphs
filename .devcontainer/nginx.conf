
events {

}

http {

    include mime.types;

    server {
        listen 8080;
        root /app;

        location / {
        }
    }

    server {
        add_header 'Access-Control-Allow-Origin' 'https://code.highcharts.com' always;

        location / {
            proxy_pass http://localhost:8080/;
        } 
    }
    
}
